# Tasks: Codeforces collector

- [ ] Add `httpx` to `packages/collector` and create the module skeleton.
- [ ] Implement the API client with the rate limiter and backoff.
- [ ] Record the API fixtures and write the client tests.
- [ ] Implement the page fetch with the on-disk cache.
- [ ] Implement the statement and sample-test parser, with fixtures and tests.
- [ ] Implement the anonymizer with the private mapping, and its tests.
- [ ] Implement the storage with schema validation on write, and its tests.
- [ ] Implement the CLI with the freshness, rating, and limit arguments.
- [ ] Implement the idempotent skip logic.
- [ ] Run the manual smoke run (3 problems) and check all acceptance criteria.
- [ ] Set the spec status to `done` and update the baseline documents.
